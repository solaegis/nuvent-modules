variables:
  member:
    type: string
    description: "Required property for TasksQueueIamMember"
  name:
    type: string
    description: "Required property for TasksQueueIamMember"
  role:
    type: string
    description: "Required property for TasksQueueIamMember"
  id:
    type: string
    description: "Optional property for TasksQueueIamMember"
    default: null
  location:
    type: string
    description: "Optional property for TasksQueueIamMember"
    default: null
  project:
    type: string
    description: "Optional property for TasksQueueIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:cloud:TasksQueueIamMember
    properties:
      member: ${var.member}
      name: ${var.name}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
