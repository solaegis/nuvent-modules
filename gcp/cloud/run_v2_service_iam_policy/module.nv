variables:
  name:
    type: string
    description: "Required property for RunV2ServiceIamPolicy"
  policy_data:
    type: string
    description: "Required property for RunV2ServiceIamPolicy"
  id:
    type: string
    description: "Optional property for RunV2ServiceIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for RunV2ServiceIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for RunV2ServiceIamPolicy"
    default: null

resources:
  main:
    type: gcp:cloud:RunV2ServiceIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
