variables:
  name:
    type: string
    description: "Required property for TasksQueueIamPolicy"
  policy_data:
    type: string
    description: "Required property for TasksQueueIamPolicy"
  id:
    type: string
    description: "Optional property for TasksQueueIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for TasksQueueIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for TasksQueueIamPolicy"
    default: null

resources:
  main:
    type: gcp:cloud:TasksQueueIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
