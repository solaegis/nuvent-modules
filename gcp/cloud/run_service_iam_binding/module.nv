variables:
  members:
    type: list
    description: "Required property for RunServiceIamBinding"
  role:
    type: string
    description: "Required property for RunServiceIamBinding"
  service:
    type: string
    description: "Required property for RunServiceIamBinding"
  id:
    type: string
    description: "Optional property for RunServiceIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for RunServiceIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for RunServiceIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:cloud:RunServiceIamBinding
    properties:
      members: ${var.members}
      role: ${var.role}
      service: ${var.service}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
