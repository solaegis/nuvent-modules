#------------------------------------------------------------------------------
# gcp/iap/web_cloud_run_service_iam_binding/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json?token=GHSAT0AAAAAADJBKTZJYHQDWBGJPIP6TMBS2KUJZ3A
#------------------------------------------------------------------------------

variables:
  cloud_run_service_name:
    type: string
    description: "Required property for WebCloudRunServiceIamBinding"
  members:
    type: list
    description: "Required property for WebCloudRunServiceIamBinding"
  role:
    type: string
    description: "Required property for WebCloudRunServiceIamBinding"
  id:
    type: string
    description: "Optional property for WebCloudRunServiceIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for WebCloudRunServiceIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for WebCloudRunServiceIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:iap:WebCloudRunServiceIamBinding
    properties:
      cloud_run_service_name: ${var.cloud_run_service_name}
      members: ${var.members}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
