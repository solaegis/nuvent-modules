variables:
  member:
    type: string
    description: "Required property for WebIamMember"
  role:
    type: string
    description: "Required property for WebIamMember"
  id:
    type: string
    description: "Optional property for WebIamMember"
    default: null
  project:
    type: string
    description: "Optional property for WebIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:iap:WebIamMember
    properties:
      member: ${var.member}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
