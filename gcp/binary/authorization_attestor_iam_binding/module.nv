variables:
  attestor:
    type: string
    description: "Required property for AuthorizationAttestorIamBinding"
  members:
    type: list
    description: "Required property for AuthorizationAttestorIamBinding"
  role:
    type: string
    description: "Required property for AuthorizationAttestorIamBinding"
  id:
    type: string
    description: "Optional property for AuthorizationAttestorIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for AuthorizationAttestorIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:binary:AuthorizationAttestorIamBinding
    properties:
      attestor: ${var.attestor}
      members: ${var.members}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  project:
    description: "Output: project"
    value: ${resources.main.project}
