#------------------------------------------------------------------------------
# gcp/clouddeploy/delivery_pipeline_iam_policy/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  name:
    type: string
    description: "Required property for DeliveryPipelineIamPolicy"
  policy_data:
    type: string
    description: "Required property for DeliveryPipelineIamPolicy"
  id:
    type: string
    description: "Optional property for DeliveryPipelineIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for DeliveryPipelineIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for DeliveryPipelineIamPolicy"
    default: null

resources:
  main:
    type: gcp:clouddeploy:DeliveryPipelineIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
