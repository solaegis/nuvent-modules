variables:
  member:
    type: string
    description: "Required property for CustomTargetTypeIamMember"
  name:
    type: string
    description: "Required property for CustomTargetTypeIamMember"
  role:
    type: string
    description: "Required property for CustomTargetTypeIamMember"
  id:
    type: string
    description: "Optional property for CustomTargetTypeIamMember"
    default: null
  location:
    type: string
    description: "Optional property for CustomTargetTypeIamMember"
    default: null
  project:
    type: string
    description: "Optional property for CustomTargetTypeIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:clouddeploy:CustomTargetTypeIamMember
    properties:
      member: ${var.member}
      name: ${var.name}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
