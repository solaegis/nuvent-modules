variables:
  job_id:
    type: string
    description: "Required property for JobIamBinding"
  members:
    type: list
    description: "Required property for JobIamBinding"
  role:
    type: string
    description: "Required property for JobIamBinding"
  id:
    type: string
    description: "Optional property for JobIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for JobIamBinding"
    default: null
  region:
    type: string
    description: "Optional property for JobIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:dataproc:JobIamBinding
    properties:
      job_id: ${var.job_id}
      members: ${var.members}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      region: ${var.region}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
