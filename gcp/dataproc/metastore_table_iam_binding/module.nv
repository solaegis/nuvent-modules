#------------------------------------------------------------------------------
# gcp/dataproc/metastore_table_iam_binding/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  database_id:
    type: string
    description: "Required property for MetastoreTableIamBinding"
  members:
    type: list
    description: "Required property for MetastoreTableIamBinding"
  role:
    type: string
    description: "Required property for MetastoreTableIamBinding"
  service_id:
    type: string
    description: "Required property for MetastoreTableIamBinding"
  table:
    type: string
    description: "Required property for MetastoreTableIamBinding"
  id:
    type: string
    description: "Optional property for MetastoreTableIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for MetastoreTableIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for MetastoreTableIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:dataproc:MetastoreTableIamBinding
    properties:
      database_id: ${var.database_id}
      members: ${var.members}
      role: ${var.role}
      service_id: ${var.service_id}
      table: ${var.table}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
