variables:
  members:
    type: list
    description: "Required property for CatalogTaxonomyIamBinding"
  role:
    type: string
    description: "Required property for CatalogTaxonomyIamBinding"
  taxonomy:
    type: string
    description: "Required property for CatalogTaxonomyIamBinding"
  id:
    type: string
    description: "Optional property for CatalogTaxonomyIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for CatalogTaxonomyIamBinding"
    default: null
  region:
    type: string
    description: "Optional property for CatalogTaxonomyIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:data:CatalogTaxonomyIamBinding
    properties:
      members: ${var.members}
      role: ${var.role}
      taxonomy: ${var.taxonomy}
      id: ${var.id}
      project: ${var.project}
      region: ${var.region}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
