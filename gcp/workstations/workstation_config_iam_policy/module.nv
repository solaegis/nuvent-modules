#------------------------------------------------------------------------------
# gcp/workstations/workstation_config_iam_policy/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  policy_data:
    type: string
    description: "Required property for WorkstationConfigIamPolicy"
  workstation_cluster_id:
    type: string
    description: "Required property for WorkstationConfigIamPolicy"
  workstation_config_id:
    type: string
    description: "Required property for WorkstationConfigIamPolicy"
  id:
    type: string
    description: "Optional property for WorkstationConfigIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for WorkstationConfigIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for WorkstationConfigIamPolicy"
    default: null

resources:
  main:
    type: gcp:workstations:WorkstationConfigIamPolicy
    properties:
      policy_data: ${var.policy_data}
      workstation_cluster_id: ${var.workstation_cluster_id}
      workstation_config_id: ${var.workstation_config_id}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
