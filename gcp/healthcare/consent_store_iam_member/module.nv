variables:
  consent_store_id:
    type: string
    description: "Required property for ConsentStoreIamMember"
  dataset:
    type: string
    description: "Required property for ConsentStoreIamMember"
  member:
    type: string
    description: "Required property for ConsentStoreIamMember"
  role:
    type: string
    description: "Required property for ConsentStoreIamMember"
  id:
    type: string
    description: "Optional property for ConsentStoreIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:healthcare:ConsentStoreIamMember
    properties:
      consent_store_id: ${var.consent_store_id}
      dataset: ${var.dataset}
      member: ${var.member}
      role: ${var.role}
      id: ${var.id}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
