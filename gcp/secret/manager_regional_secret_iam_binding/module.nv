variables:
  members:
    type: list
    description: "Required property for ManagerRegionalSecretIamBinding"
  role:
    type: string
    description: "Required property for ManagerRegionalSecretIamBinding"
  secret_id:
    type: string
    description: "Required property for ManagerRegionalSecretIamBinding"
  id:
    type: string
    description: "Optional property for ManagerRegionalSecretIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for ManagerRegionalSecretIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for ManagerRegionalSecretIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:secret:ManagerRegionalSecretIamBinding
    properties:
      members: ${var.members}
      role: ${var.role}
      secret_id: ${var.secret_id}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
