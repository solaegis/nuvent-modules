variables:
  name:
    type: string
    description: "Required property for BackupRestorePlanIamPolicy"
  policy_data:
    type: string
    description: "Required property for BackupRestorePlanIamPolicy"
  id:
    type: string
    description: "Optional property for BackupRestorePlanIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for BackupRestorePlanIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for BackupRestorePlanIamPolicy"
    default: null

resources:
  main:
    type: gcp:gke:BackupRestorePlanIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
