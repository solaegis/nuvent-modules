#------------------------------------------------------------------------------
# gcp/gke/hub_membership_iam_member/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  member:
    type: string
    description: "Required property for HubMembershipIamMember"
  membership_id:
    type: string
    description: "Required property for HubMembershipIamMember"
  role:
    type: string
    description: "Required property for HubMembershipIamMember"
  id:
    type: string
    description: "Optional property for HubMembershipIamMember"
    default: null
  location:
    type: string
    description: "Optional property for HubMembershipIamMember"
    default: null
  project:
    type: string
    description: "Optional property for HubMembershipIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:gke:HubMembershipIamMember
    properties:
      member: ${var.member}
      membership_id: ${var.membership_id}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
