#------------------------------------------------------------------------------
# gcp/secure/source_manager_repository_iam_policy/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json?token=GHSAT0AAAAAADJBKTZJYHQDWBGJPIP6TMBS2KUJZ3A
#------------------------------------------------------------------------------

variables:
  policy_data:
    type: string
    description: "Required property for SourceManagerRepositoryIamPolicy"
  repository_id:
    type: string
    description: "Required property for SourceManagerRepositoryIamPolicy"
  id:
    type: string
    description: "Optional property for SourceManagerRepositoryIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for SourceManagerRepositoryIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for SourceManagerRepositoryIamPolicy"
    default: null

resources:
  main:
    type: gcp:secure:SourceManagerRepositoryIamPolicy
    properties:
      policy_data: ${var.policy_data}
      repository_id: ${var.repository_id}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
