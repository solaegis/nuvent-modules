variables:
  data_scan_id:
    type: string
    description: "Required property for DatascanIamBinding"
  members:
    type: list
    description: "Required property for DatascanIamBinding"
  role:
    type: string
    description: "Required property for DatascanIamBinding"
  id:
    type: string
    description: "Optional property for DatascanIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for DatascanIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for DatascanIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:dataplex:DatascanIamBinding
    properties:
      data_scan_id: ${var.data_scan_id}
      members: ${var.members}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
