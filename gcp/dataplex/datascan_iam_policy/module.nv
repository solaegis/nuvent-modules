#------------------------------------------------------------------------------
# gcp/dataplex/datascan_iam_policy/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  data_scan_id:
    type: string
    description: "Required property for DatascanIamPolicy"
  policy_data:
    type: string
    description: "Required property for DatascanIamPolicy"
  id:
    type: string
    description: "Optional property for DatascanIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for DatascanIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for DatascanIamPolicy"
    default: null

resources:
  main:
    type: gcp:dataplex:DatascanIamPolicy
    properties:
      data_scan_id: ${var.data_scan_id}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
