#------------------------------------------------------------------------------
# gcp/dataplex/zone_iam_binding/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json?token=GHSAT0AAAAAADJBKTZJYHQDWBGJPIP6TMBS2KUJZ3A
#------------------------------------------------------------------------------

variables:
  dataplex_zone:
    type: string
    description: "Required property for ZoneIamBinding"
  lake:
    type: string
    description: "Required property for ZoneIamBinding"
  members:
    type: list
    description: "Required property for ZoneIamBinding"
  role:
    type: string
    description: "Required property for ZoneIamBinding"
  id:
    type: string
    description: "Optional property for ZoneIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for ZoneIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for ZoneIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:dataplex:ZoneIamBinding
    properties:
      dataplex_zone: ${var.dataplex_zone}
      lake: ${var.lake}
      members: ${var.members}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  location:
    description: "Output: location"
    value: ${resources.main.location}
  project:
    description: "Output: project"
    value: ${resources.main.project}
