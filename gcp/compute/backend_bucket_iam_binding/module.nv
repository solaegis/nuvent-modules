variables:
  members:
    type: list
    description: "Required property for BackendBucketIamBinding"
  name:
    type: string
    description: "Required property for BackendBucketIamBinding"
  role:
    type: string
    description: "Required property for BackendBucketIamBinding"
  id:
    type: string
    description: "Optional property for BackendBucketIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for BackendBucketIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:compute:BackendBucketIamBinding
    properties:
      members: ${var.members}
      name: ${var.name}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
