variables:
  name:
    type: string
    description: "Required property for InstanceTemplateIamPolicy"
  policy_data:
    type: string
    description: "Required property for InstanceTemplateIamPolicy"
  id:
    type: string
    description: "Optional property for InstanceTemplateIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for InstanceTemplateIamPolicy"
    default: null

resources:
  main:
    type: gcp:compute:InstanceTemplateIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      project: ${var.project}
