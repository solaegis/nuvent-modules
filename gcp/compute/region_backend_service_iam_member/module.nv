variables:
  member:
    type: string
    description: "Required property for RegionBackendServiceIamMember"
  name:
    type: string
    description: "Required property for RegionBackendServiceIamMember"
  role:
    type: string
    description: "Required property for RegionBackendServiceIamMember"
  id:
    type: string
    description: "Optional property for RegionBackendServiceIamMember"
    default: null
  project:
    type: string
    description: "Optional property for RegionBackendServiceIamMember"
    default: null
  region:
    type: string
    description: "Optional property for RegionBackendServiceIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:compute:RegionBackendServiceIamMember
    properties:
      member: ${var.member}
      name: ${var.name}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      region: ${var.region}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  project:
    description: "Output: project"
    value: ${resources.main.project}
  region:
    description: "Output: region"
    value: ${resources.main.region}
