variables:
  members:
    type: list
    description: "Required property for RegionDiskIamBinding"
  name:
    type: string
    description: "Required property for RegionDiskIamBinding"
  role:
    type: string
    description: "Required property for RegionDiskIamBinding"
  id:
    type: string
    description: "Optional property for RegionDiskIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for RegionDiskIamBinding"
    default: null
  region:
    type: string
    description: "Optional property for RegionDiskIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:compute:RegionDiskIamBinding
    properties:
      members: ${var.members}
      name: ${var.name}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      region: ${var.region}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
