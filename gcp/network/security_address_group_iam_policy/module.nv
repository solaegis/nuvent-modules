variables:
  name:
    type: string
    description: "Required property for SecurityAddressGroupIamPolicy"
  policy_data:
    type: string
    description: "Required property for SecurityAddressGroupIamPolicy"
  id:
    type: string
    description: "Optional property for SecurityAddressGroupIamPolicy"
    default: null
  location:
    type: string
    description: "Optional property for SecurityAddressGroupIamPolicy"
    default: null
  project:
    type: string
    description: "Optional property for SecurityAddressGroupIamPolicy"
    default: null

resources:
  main:
    type: gcp:network:SecurityAddressGroupIamPolicy
    properties:
      name: ${var.name}
      policy_data: ${var.policy_data}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
