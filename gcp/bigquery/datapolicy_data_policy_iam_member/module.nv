variables:
  data_policy_id:
    type: string
    description: "Required property for DatapolicyDataPolicyIamMember"
  member:
    type: string
    description: "Required property for DatapolicyDataPolicyIamMember"
  role:
    type: string
    description: "Required property for DatapolicyDataPolicyIamMember"
  id:
    type: string
    description: "Optional property for DatapolicyDataPolicyIamMember"
    default: null
  location:
    type: string
    description: "Optional property for DatapolicyDataPolicyIamMember"
    default: null
  project:
    type: string
    description: "Optional property for DatapolicyDataPolicyIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:bigquery:DatapolicyDataPolicyIamMember
    properties:
      data_policy_id: ${var.data_policy_id}
      member: ${var.member}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
