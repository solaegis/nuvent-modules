#------------------------------------------------------------------------------
# gcp/scc/v2_organization_source_iam_binding/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json
#------------------------------------------------------------------------------

variables:
  members:
    type: list
    description: "Required property for V2OrganizationSourceIamBinding"
  organization:
    type: string
    description: "Required property for V2OrganizationSourceIamBinding"
  role:
    type: string
    description: "Required property for V2OrganizationSourceIamBinding"
  source:
    type: string
    description: "Required property for V2OrganizationSourceIamBinding"
  id:
    type: string
    description: "Optional property for V2OrganizationSourceIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:scc:V2OrganizationSourceIamBinding
    properties:
      members: ${var.members}
      organization: ${var.organization}
      role: ${var.role}
      source: ${var.source}
      id: ${var.id}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
