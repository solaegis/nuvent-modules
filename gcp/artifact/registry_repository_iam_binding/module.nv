variables:
  members:
    type: list
    description: "Required property for RegistryRepositoryIamBinding"
  repository:
    type: string
    description: "Required property for RegistryRepositoryIamBinding"
  role:
    type: string
    description: "Required property for RegistryRepositoryIamBinding"
  id:
    type: string
    description: "Optional property for RegistryRepositoryIamBinding"
    default: null
  location:
    type: string
    description: "Optional property for RegistryRepositoryIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for RegistryRepositoryIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:artifact:RegistryRepositoryIamBinding
    properties:
      members: ${var.members}
      repository: ${var.repository}
      role: ${var.role}
      id: ${var.id}
      location: ${var.location}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}
