variables:
  member:
    type: string
    description: "Required property for AnalysisNoteIamMember"
  note:
    type: string
    description: "Required property for AnalysisNoteIamMember"
  role:
    type: string
    description: "Required property for AnalysisNoteIamMember"
  id:
    type: string
    description: "Optional property for AnalysisNoteIamMember"
    default: null
  project:
    type: string
    description: "Optional property for AnalysisNoteIamMember"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:container:AnalysisNoteIamMember
    properties:
      member: ${var.member}
      note: ${var.note}
      role: ${var.role}
      id: ${var.id}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  project:
    description: "Output: project"
    value: ${resources.main.project}
