#------------------------------------------------------------------------------
# gcp/beyondcorp/security_gateway_application_iam_binding/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json?token=GHSAT0AAAAAADJBKTZJYHQDWBGJPIP6TMBS2KUJZ3A
#------------------------------------------------------------------------------

variables:
  application_id:
    type: string
    description: "Required property for SecurityGatewayApplicationIamBinding"
  members:
    type: list
    description: "Required property for SecurityGatewayApplicationIamBinding"
  role:
    type: string
    description: "Required property for SecurityGatewayApplicationIamBinding"
  security_gateway_id:
    type: string
    description: "Required property for SecurityGatewayApplicationIamBinding"
  id:
    type: string
    description: "Optional property for SecurityGatewayApplicationIamBinding"
    default: null
  project:
    type: string
    description: "Optional property for SecurityGatewayApplicationIamBinding"
    default: null
  condition:
    type: list
    description: "Nested block: condition"
    default: []

resources:
  main:
    type: gcp:beyondcorp:SecurityGatewayApplicationIamBinding
    properties:
      application_id: ${var.application_id}
      members: ${var.members}
      role: ${var.role}
      security_gateway_id: ${var.security_gateway_id}
      id: ${var.id}
      project: ${var.project}
      dynamic:
        condition:
          for_each: ${var.condition}
          content:
            description: ${each.value.description}
            expression: ${each.value.expression}
            title: ${each.value.title}

outputs:
  etag:
    description: "Output: etag"
    value: ${resources.main.etag}
  id:
    description: "Output: id"
    value: ${resources.main.id}
  project:
    description: "Output: project"
    value: ${resources.main.project}
