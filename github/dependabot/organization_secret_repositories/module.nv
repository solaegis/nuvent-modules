#------------------------------------------------------------------------------
# github/dependabot/organization_secret_repositories/module.nv
# yaml-language-server: $schema=https://raw.githubusercontent.com/solaegis/nuvent/refs/heads/main/tools/vscode-extension/schemas/nuvent-manifest.schema.json?token=GHSAT0AAAAAADJBKTZJYHQDWBGJPIP6TMBS2KUJZ3A
#------------------------------------------------------------------------------

variables:
  secret_name:
    type: string
    description: "Name of the existing secret."
  selected_repository_ids:
    type: list
    description: "An array of repository ids that can access the organization secret."
  id:
    type: string
    description: "Optional property for OrganizationSecretRepositories"
    default: null

resources:
  main:
    type: github:dependabot:OrganizationSecretRepositories
    properties:
      secret_name: ${var.secret_name}
      selected_repository_ids: ${var.selected_repository_ids}
      id: ${var.id}

outputs:
  id:
    description: "Output: id"
    value: ${resources.main.id}
